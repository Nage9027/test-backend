# ----------------------------------------------------------------------------
# Frontend environment template — copy to `.env` and fill in real values.
# ----------------------------------------------------------------------------

# Base URL of the API. Vite exposes only variables prefixed with VITE_.
VITE_API_URL=http://localhost:5000/api
